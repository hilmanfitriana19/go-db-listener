package helpers

var (
	slHN = "ServiceLogHelper"
)

var (
	emptyEncryptionKey     = "empty encryption key"
	errDecodedMessage      = "failed to decode message"
	errDecryptText         = "failed to decrypt text"
	errEncryptText         = "failed to encrypt text"
	errMarshalServiceLog   = "failed to marshal service log"
	errUploadFileToStorage = "failed to upload file to storage"
)
